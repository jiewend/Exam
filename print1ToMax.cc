#include <iostream> 
#include <string.h> 
using namespace std;

bool incrementNum(char *strNum, int n){
	int numLen = strlen(strNum);
	for (unsigned i = 0; i < numLen; i++) {
		
	} 
	
}
int main(){
	// int n ;
	// cin>>n;

	// char strNum[n+1] = {'\0'};
	char num = 0;
	cout<<" ,"<<(int)num<<", "<<(int)num +1<<endl;



}
